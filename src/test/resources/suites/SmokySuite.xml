<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd" >
<suite name="SmokySuite">
    <test name="SmokySuite">
        <classes>
            <class name="com.internet.tests.IFrameTest"/>
        </classes>
    </test>
</suite>
        <!--TestNG ищет полностью квалифицированное имя класса — это package + имя класса, а не путь к файлу на диске.
     То есть он всегда стартует из src/test/java (для тестов) или src/main/java (для кода), потому что эти папки уже на classpath.

     mvn clean test "-DsuiteXmlFile=src/test/resources/suites/master-suite.xml"


     -->